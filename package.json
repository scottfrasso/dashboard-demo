{
  "name": "dash-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "private",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "lint": "yarn workspace @dashboard/front-end run lint",
    "build": "yarn workspace @dashboard/front-end run build && yarn workspace @dashboard/front-end run export",
    "ci": "yarn run lint && yarn run build"
  }
}
