{
  "name": "dash-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "private",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "lint": "yarn workspaces run lint",
    "build": "yarn workspace @dashboard/dtos build && yarn workspace @dashboard/api run build && yarn workspace @dashboard/front-end run build && yarn workspace @dashboard/front-end run export",
    "ci": "yarn run lint && yarn run build",
    "start:api": "yarn workspace @dashboard/api run start:dev",
    "start:front-end": "yarn workspace @dashboard/front-end run start:dev",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint-plugin-react": "7.31.11",
    "gts": "^3.1.1",
    "husky": "^8.0.0"
  }
}
